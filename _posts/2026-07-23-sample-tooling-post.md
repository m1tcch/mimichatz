---
layout: post
title: "sample: writing about tooling"
description: >
  A placeholder post showing the layout for tool reviews and project notes.
categories: [tooling]
tags: [sample]
---

> Another **sample post** — delete once you have real content.

Tooling posts tend to be shorter and more practical: what the tool does, how to
run it, and what to watch out for.

## install

```bash
git clone https://github.com/example/some-tool.git
cd some-tool
pip install -r requirements.txt
```

## usage

```bash
python some_tool.py --input capture.pcap --output findings.json
```

Flags and paths sit nicely inline: run with `--verbose` when debugging, and
results land in `findings.json`.

## verdict

A short closing take. This post is in the `tooling` category, so it shows up
at [/tooling/](/tooling/) — linked straight from the sidebar.
